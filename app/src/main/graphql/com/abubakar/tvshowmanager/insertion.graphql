mutation CreateMovie($input: CreateMovieInput!) {
    createMovie(input: $input){
        clientMutationId
        movie {
            title
            releaseDate
            seasons
        }
    }
}
